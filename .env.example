# Clerk Authentication (REQUIRED)
# Get from: https://dashboard.clerk.com/
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
CLERK_SECRET_KEY=your_clerk_secret_key_here

# AI API Key - REQUIRED
# Get from: https://console.x.ai/
XAI_API_KEY=your_xai_api_key_here

# Database Configuration (optional - uses in-memory storage if not set)
# Format: postgresql://user:password@localhost:5432/database_name
DATABASE_URL=postgresql://user:password@localhost:5432/whispersandflames
CRON_SECRET=your_cron_secret_here

# Disable Database (optional - set to 'true' to force in-memory storage)
# Use this to disable database even when DATABASE_URL is configured
# Useful for troubleshooting database issues without removing DATABASE_URL
DISABLE_DATABASE=false

# NOTE: DISABLE_SSL_VALIDATION was documented here but is NOT implemented —
# nothing in src/ reads it. src/lib/storage-pg.ts hardcodes
# `ssl: NODE_ENV === 'production' ? { rejectUnauthorized: true } : false`.
# If your Postgres provider uses a self-signed certificate (several managed
# providers do), connections will fail in production and setting this variable
# will not help. Implement the flag deliberately rather than assuming it works.

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:9002

# Environment
NODE_ENV=development

# Optional error monitoring
SENTRY_DSN=
