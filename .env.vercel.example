# Vercel Environment Variables Template
# Copy these to your Vercel project settings: https://vercel.com/dashboard/[project]/settings/environment-variables

# ==============================================
# REQUIRED - AI API Key
# ==============================================
# Get from: https://console.x.ai/
XAI_API_KEY=your_xai_api_key_here

# ==============================================
# REQUIRED - Database (Vercel Postgres)
# ==============================================
# Automatically set when you add Vercel Postgres storage
# Format: postgres://default:[password]@[host]/verceldb
POSTGRES_URL=automatically_set_by_vercel

# Alternative variable names (Vercel Postgres provides both)
# IMPORTANT: Copy the POSTGRES_URL value here - the app uses DATABASE_URL
# Variable expansion (${POSTGRES_URL}) doesn't work in Vercel UI
DATABASE_URL=<copy_the_postgres_url_value_here>

# ==============================================
# REQUIRED - Session Security
# ==============================================
# Generate with: openssl rand -base64 32
# IMPORTANT: Use a strong random secret for production!
SESSION_SECRET=your_32_character_random_secret_here

# ==============================================
# REQUIRED - Application URL
# ==============================================
# Set to your Vercel deployment URL
# Format: https://your-app.vercel.app (no trailing slash)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# ==============================================
# OPTIONAL - Storage Configuration
# ==============================================
# Vercel Postgres uses 'postgres' mode automatically
# Only set if you want to force in-memory storage (not recommended for production)
# STORAGE_MODE=postgres

# ==============================================
# AUTOMATIC - Vercel System Variables
# ==============================================
# These are automatically set by Vercel, no need to configure:
# - VERCEL=1
# - VERCEL_ENV (production, preview, or development)
# - VERCEL_URL (deployment URL)
# - VERCEL_GIT_COMMIT_SHA
# - VERCEL_GIT_COMMIT_REF
# - NODE_ENV=production (automatically set)

# ==============================================
# INSTRUCTIONS
# ==============================================
# 1. Go to: https://vercel.com/dashboard/[your-project]/settings/environment-variables
# 2. Add each variable above (except the AUTOMATIC section)
# 3. For DATABASE_URL: Add Vercel Postgres storage first, it will auto-populate
# 4. For NEXT_PUBLIC_APP_URL: Use your production domain or *.vercel.app URL
# 5. Sensitive variables (API keys, secrets) should be encrypted by Vercel automatically
